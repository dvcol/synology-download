const { search, pause, getFileIcon, resume, cancel, download, open, show, showDefaultFolder, erase, onCreated } = chrome.downloads;

export { search, pause, getFileIcon, resume, cancel, download, open, show, showDefaultFolder, erase, onCreated };
